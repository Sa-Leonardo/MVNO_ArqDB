import { Card, CardContent } from "@/components/ui/Card";

export function ReportsPage() {
  return (
    <Card>
      <CardContent>
        <h1 className="text-xl font-bold text-ink">Relatórios</h1>
        <p className="mt-2 text-sm text-slate-500">Área preparada para análises de consumo, vendas e ativações.</p>
      </CardContent>
    </Card>
  );
}
